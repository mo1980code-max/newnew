# ملاحظات الترقية — Allah Clock Live Wallpaper

تحديث شامل للمشروع: SDK 36 + الإعلانات (Native / Rewarded / Interstitial / Banner) + تنظيف الكود.

---

## 1) خطوة يدوية واحدة مطلوبة قبل أول بناء

ملف `gradle/wrapper/gradle-wrapper.jar` الموجود في المستودع قديم جدًا (من Gradle **2.10** سنة 2015)،
بينما `gradle-wrapper.properties` يشير الآن إلى Gradle **8.13**. قد لا يستطيع الـ jar القديم تحميل
التوزيع الجديد.

**الحل (أي واحدة تكفي):**

- افتح المشروع في **Android Studio** (Giraffe أو أحدث) → عند طلب المزامنة اختر
  *"Upgrade Gradle wrapper"* أو دعه يعيد توليد الـ wrapper تلقائيًا، **أو**
- نفّذ من الطرفية إذا كان Gradle مثبّتًا عندك:

  ```bash
  gradle wrapper --gradle-version 8.13 --distribution-type bin
  ```

بعد ذلك التزم بالملفات الثلاثة المولّدة:
`gradlew`, `gradlew.bat`, `gradle/wrapper/gradle-wrapper.jar`.

> ملاحظة: الـ JDK المطلوب هو **17** (إلزامي مع AGP 8.x). Android Studio الحديث يأتي معه
> (JBR 17/21) ولا يحتاج تثبيتًا منفصلًا.

---

## 2) قبل النشر على Google Play — مهم جدًا

| البند | المكان | ما يجب فعله |
|---|---|---|
| **أرقام الإعلانات** | `ads/AdConfig.java` + `AndroidManifest.xml` | القيم الحالية هي **أرقام اختبار رسمية من Google**. استبدل الخمسة (App ID + Banner + Interstitial + Rewarded + Native) بأرقامك من [apps.admob.com](https://apps.admob.com). رقم التطبيق يجب أن يتطابق في الملفين. |
| **versionCode** | `app/build.gradle` | حاليًا `2` / `1.1`. ارفعه فوق آخر رقم منشور على Play Console وإلا سيُرفض الرفع. |
| **أجهزة الاختبار** | `AdConfig.TEST_DEVICE_HASHED_IDS` | بعد استخدام أرقام حقيقية، أضف الـ hashed IDs لهواتفك (تظهر في Logcat بوسم `Ads`) حتى لا تنقر على إعلان حي بالخطأ — هذا يُعتبر مخالفة سياسة. |
| **نموذج Data safety** | Play Console | أعلن عن: Advertising ID, Device/Other IDs, Approximate location, Crash/Diagnostics. واذكر مشاركة البيانات مع Google (AdMob). |
| **إعلانات مخصّصة** | Play Console → *App content → Ads* | اختر "يحتوي على إعلانات". |
| **UMP / موافقة المستخدم** | مفعّل مسبقًا في `AdManager` | أضف رابطًا لـ *privacy policy* في إعدادات AdMob حتى تظهر رسالة الموافقة لمستخدمي EEA/UK/CH. |
| **توقيع الإصدار** | — | `minifyEnabled true` + `shrinkResources true` مفعّلان في `release`. جرّب بناء release ونصّبه على جهاز حقيقي قبل الرفع. |

---

## 3) ما تغيّر في المشروع

### البناء والإصدارات
- `compileSdk 36` / `targetSdk 36` (متطلب Play لتحديثات التطبيقات منذ 31 آب 2026).
- `minSdk 23` — الحد الأدنى لإعلانات Google Mobile Ads 25.x ومكتبات AndroidX الحالية.
- AGP `8.13.2` + Gradle `8.13` + Java `17`.
- حذف `jcenter()` (مغلق نهائيًا) وإضافة `jitpack` (لمكتبة colorpicker).
- إزالة مكتبات غير مستخدمة: material، constraintlayout، volley، picasso، simplecropview،
  filedownloader، connector، smarttablayout، spinkit.

### الإعلانات (كلها تمر عبر `ads/AdManager.java`)
1. **Native** — بطاقة إعلانية في **منتصف** قوائم الساعات والخلفيات
   (`NativeAdListAdapter` يغلف الـ adapter ويُدخل بطاقة واحدة عند نسبة `0.5` بعرض كامل).
2. **Rewarded** — يفتح الساعات الذهبية (آخر **3** عناصر من كل قائمة: Analog / Digital / Smart).
   الفتح **للجلسة الحالية فقط** ويعود مقفولًا بعد إغلاق التطبيق.
3. **Interstitial** — يُعرض **فقط بعد نجاح تطبيق الخلفية**، ولا يُعرض أبدًا عند الإقلاع أو الخروج.
4. **Banner** — adaptive banner في **أسفل شاشات الإعدادات الفرعية فقط**
   (ClockFuntion، ClockCard، Wallpaper، WallpaperCategory). لا بانر في الشاشة الرئيسية ولا في شاشة المحرر/التطبيق.

### سياسة Google Play
- **UMP consent** قبل تهيئة SDK وقبل أي طلب إعلان.
- زر/أيقونة **Privacy options** تظهر فقط عندما تفرضها Google، وتعيد فتح نموذج الموافقة.
- الإعلانات الأصلية داخل `NativeAdView` مع أيقونة AdChoices وشارة "Ad" واضحة.
- لا إعلانات قبل أن يستطيع المستخدم التفاعل، ولا إعلانات مفاجئة عند الخروج.
- `UNLOCK_WHEN_REWARDED_UNAVAILABLE = true`: إذا فشل تحميل الإعلان المكافئ لا يبقى المستخدم عالقًا.
- حُذفت صلاحيات التخزين غير الضرورية (`READ/WRITE_EXTERNAL_STORAGE`) — الصور الملتقطة من المعرض
  تُنسخ إلى مساحة التطبيق الخاصة عبر `ContentResolver`، والخلفيات تُحمّل إلى `getExternalCacheDir()`.
- حُذف `AD_MANAGER_APP` من الـ manifest (كان يمنع عمل وحدات AdMob العادية).
- كل النصوص في `strings.xml`، وكل عنصر أيقوني له `contentDescription` (متطلب إمكانية الوصول).

### تنظيف الكود
- حذف ملفات غير مستخدمة: `AdAdmob.java`, `utils/RealPathUtil.java`,
  `rvadapter/RecyclerViewAdapterWrapper.java`, `res/anim/bottom_animation.xml`, `res/values/arrays.xml`.
- استبدال الواجهات المهجورة: `ProgressDialog` → `UiCompat.showLoading`،
  `startActivityForResult` → `ActivityResultLauncher`،
  `setSystemUiVisibility` → `WindowInsetsControllerCompat`،
  `new Handler()` → `new Handler(Looper.getMainLooper())`،
  `InputDeviceCompat.SOURCE_ANY` → `Color.WHITE`،
  `switch (view.getId())` → `if/else` (ثوابت R لم تعد compile-time constants).
- **Edge-to-edge** مفروض الآن من النظام في targetSdk 35+؛ تمت معالجته عبر `UiCompat.applyEdgeToEdge`
  و `applyImmersive` حتى لا يختفي المحتوى تحت أشرطة النظام.

---

## 4) هيكل مجلد الإعلانات

```
ads/
├── AdConfig.java             كل الثوابت (أرقام الوحدات، عدد العناصر الذهبية، موضع الـ Native)
├── AdManager.java            UMP + تهيئة SDK + Interstitial + Rewarded + Native
├── BannerAdController.java   بانر adaptive مع إعادة المحاولة عند onResume
├── NativeAdListAdapter.java  RecyclerView.Adapter wrapper يُدخل بطاقة Native في المنتصف
├── NativeAdViewHolder.java   ربط NativeAdView (MediaView + AdChoices + شارة Ad)
├── PremiumBadge.java         شارة القفل الذهبي + بوابة الفتح عند النقر
├── PremiumManager.java       حالة الفتح (جلسة/مؤقت) + تحديد العناصر المميزة
└── RewardedUnlockHelper.java حوار التوضيح ثم تشغيل الإعلان المكافئ
```

---

## 5) الخلفيات: محلية 100% — بلا أي مصدر خارجي

تم حذف الكتالوج البعيد بالكامل (`wallpaper.json` من dopewalls و `wallpapernew.json` من Flickr)
لأنه كان: (أ) روابط صور لجهات أخرى بلا ترخيص = خطر سياسة الملكية الفكرية، (ب) معتمدًا على خوادم
قد تختفي أو يتغير محتواها في أي لحظة.

**البنية الجديدة:**

| المكوّن | الدور |
|---|---|
| `res/drawable-nodpi/wp_*.jpg` | صور الخلفيات نفسها، مضمّنة داخل الـ APK (لا تُقاس بكثافة الشاشة) |
| `utils/WallpaperCatalog.java` | المصدر الوحيد للحقيقة: الفئات + ترتيبها + صورها |
| `model/WallpaperCategory.java` / `WallpaperItem.java` | نموذج بسيط بلا Gson ولا Parcelable |
| `SetWallpaperActivity` | يفكّ الرسم إلى ملف JPEG خاص بالتطبيق ثم يطبّقه/يشاركه — بلا تنزيل |

**لإضافة خلفية:** ضع jpg في `res/drawable-nodpi` وأضف `R.drawable.xxx` إلى المصفوفة المناسبة
في `WallpaperCatalog`.
**لإضافة فئة:** أضف عنوانًا في `strings.xml` وسطر `category(...)` واحدًا في `build()`.

**مكتبات حُذفت مع هذه الخطوة:** `okdownload`، `okhttp` (لا تنزيل بعد الآن).
بقي `gson` لأنه يُستخدم فقط في `TinyDB` لحفظ نموذج الساعة المختارة.

> **تنويه مهم:** صور الأماكن المقدسة الحالية **رسومات فنية مولّدة بالذكاء الاصطناعي** وليست
> صورًا فوتوغرافية حقيقية، وُضعت بأسلوب ليلي/سيلويت محترم بلا أشخاص وبلا نصوص. يمكنك استبدال
> أي منها بصور تملك حقوقها بوضع الملف بنفس الاسم داخل `drawable-nodpi`.

---

## 6) الميزات الإسلامية الثلاث + اكتمال العشرين خلفية

### التاريخ الهجري
- `utils/HijriDate.java`: على API 24+ يستخدم `android.icu` بحساب **أم القرى** الرسمي،
  وعلى API 23 حساب تبوّلي احتياطي (قد يختلف يومًا واحدًا — مقبول لسطر زخرفي).
- يظهر أعلى الخلفية الحية عندما يفعّله المستخدم من المحرر.

### الأذكار المتغيرة تلقائيًا
- `res/values/islamic_content.xml`: مصفوفة `adhkar` (14 ذكرًا صحيحًا قصيرًا).
- `WallpaperOverlayView` يختار الذكر تلقائيًا بمعادلة الوقت (يتغير كل 3 ساعات) —
  بلا مؤقّتات وبلا بطارية إضافية، ويتغيّر حتى لو فُتح الجهاز بعد غياب.
- الخط.used هو خط النظام حتى تُشكَّل العربية صحيحًا على كل الأجهزة.

### بوصلة القبلة
- `activity/QiblaActivity` + `viewUtils/CompassView` + `utils/QiblaUtil`.
- **بلا صلاحية موقع**: جدول 73 مدينة مضمّن (`QiblaUtil.CITY_COORDS`)، والميل يُحسب
  بمعادلة الدائرة العظمى إلى الكعبة (21.422487, 39.826206)، والاتجاه من حسّاس
  `ROTATION_VECTOR` مع تنعيم. تعمل بدون إنترنت تمامًا.
- تُعرض المسافة بالكيلومترات، وال_city_ المختار يُحفظ في التفضيلات.
- شاشة فرعية → فيها بانر أسفلها مثل بقية الشاشات الفرعية.

### التحكم
- زر جديد (منزلقات) في شريط المحرر السفلي يفتح `dialog_wallpaper_options`:
  تشغيل/إيقاف التاريخ الهجري والأذكار. المفاتيح تُحفظ في `TinyDB`
  (`showHijri` / `showDhikr`) وتقرأها الخلفية الحية والمحرر مباشرة (معاينة فورية).

### الخلفيات
اكتملت العشرون: 5 الكعبة والحرم المكي + 5 الحرم المدني + 5 الأقصى والقدس +
5 مساجد ومعالم = **5 فئات / 27 خلفية** كلها داخل `res/drawable-nodpi` (~2.9 MB).

---

## 7) ودجت الشاشة الرئيسية (2×2 و 4×2)

- `widget/ClockWidgetProvider` (أساس) + `ClockWidgetSquare` (2×2) + `ClockWidgetWide` (4×2).
- **نفس تصاميم الساعات حرفيًا**: `utils/WidgetClockRenderer` ينشئ نفس الـ view الذي تستخدمه
  الخلفية الحية (`AnalogClock` / `SmartClockPreview` / `TextClockPreview`) ويضبطه بنفس
  الإعدادات (`setPosition` / `setClockSize` / `config`) ثم يرسمه على Bitmap —
  فلا يوجد أي تكرار للتصاميم، وأي نمط ساعة جديد يصل للودجت تلقائيًا.
- الودجت يقرأ اختيار المستخدم الحالي من التفضيلات (`clockType` / `textClockPosition` /
  `clocks` / الألوان)، والنقر عليه يفتح شاشة اختيار الساعات.
- الودجت العريض يعرض أيضًا **التاريخ الهجري + الذكر الحالي** (نفس دالة
  `WallpaperOverlayView.currentDhikr` المستخدمة في الخلفية → نفس الذكر دائمًا في المكانين).
- **قابل للتغيير الحر**: `resizeMode="horizontal|vertical"` والرسم يتبع المقاس الفعلي من
  `AppWidgetManager.getAppWidgetOptions`، لذا 2×2 و4×2 مجرد نقطتي بداية وليس حدًا.
- **نموذج البطارية**: إعادة رسم **مرة كل دقيقة** عبر `AlarmManager.set` واحد غير مُوقِظ
  (يُؤجَّل أثناء Doze حين لا يرى أحد الشاشة)، + تحديث فوري عند: تشغيل الشاشة، تغيّر الوقت،
  تغيّر المنطقة الزمنية. أرخص بأضعاف من إعادة رسم الخلفية الحية كل ثانية.
- **بلا أي صلاحيات جديدة.**

### عن شاشة القفل — بصراحة
أندرويد الخام منذ 5.0 **أزال ودجات شاشة القفل**؛ لا توجد واجهة برمجية لها في النسخة الحديثة.
لذلك:
- أعلنّا `widgetCategory="home_screen|keyguard"` حتى تتيح الشركات المصنّعة (OEM) التي ما زالت
  تدعم الوضع على شاشة القفل استخدامه.
- البديل الحقيقي الذي يعمل على شاشة القفل في أندرويد الحديث هو **الخلفية الحية نفسها**
  أو وضع AOD، وكلاهما موجود/مدعوم في التطبيق.

---

## 8) محدّد الإطارات (FPS Limiter) + إعلانات فتح التطبيق

### وضع توفير الطاقة
- `utils/FrameRate.java` + مفتاح `powerSaver` في حوار خيارات المحرر (الافتراضي = مفعّل).
- **موفّر (الافتراضي)**: إعادة رسم الخلفية مرة واحدة كل ثانية وعقرب الثواني يقفز كل ثانية —
  وهو نفس سلوك التطبيق المنشور اليوم تمامًا، فلا يشعر المستخدمون القدامى بأي تغيير.
- **انسيابي**: عقرب ثواني يلتف باستمرار بمعدل ~30 إطارًا/ثانية (opt-in لمن يفضل الجمال).
- المحرك هو الذي يقود الإطارات وحده: أُلغيت حلقة الـ800ms الداخلية في `AnalogClock` داخل
  الخلفية (`setAutoUpdate(false)` + `setTime()` كل إطار) حتى لا تتعارض حلقتان.
- الساعات الرقمية والذكية لا تتأثر بالوضع الانسيابي (الدقيقة فقط تتغير) — توفير تلقائي.

### App Open Ads — داخل حدود السياسة حرفيًا
- `ads/AppOpenAdController.java` + رقم اختبار جوجل الرسمي في `AdConfig.APP_OPEN_UNIT_ID`.
- يُعرض **فقط** عند عودة التطبيق للمقدمة (أو إقلاع بارد) **وفقط على الشاشة الرئيسية** —
  لا يظهر فوق مهمة جارية (تطبيق خلفية مثلًا)، ولا عند الخروج، ولا فوق نموذج موافقة UMP.
- تتبع المقدمة/الخلفية عبر `ActivityLifecycleCallbacks` في `AppClass` بدون أي مكتبة إضافية.
- **تبريد 4 ساعات** محفوظ في التفضيلات + عدم التكديس: علم `FULL_SCREEN_ACTIVE` في `AdManager`
  يمنع تزامنه مع interstitial أو rewarded.
- يُحمَّل مسبقًا في الخلفية بعد تهيئة الـ SDK؛ إن لم يكن جاهزًا عند الفتح لا يُعرض شيء إطلاقًا
  ويبدأ تحميل للزيارة التالية — المستخدم لا ينتظر إعلانًا أبدًا.

---

## 9) السبحة الإلكترونية العائمة (Floating Tasbeeh)

- `service/FloatingTasbeehService.java` + `res/layout/view_floating_tasbeeh.xml`.
- **تشغيل اختياري 100%** من أيقونة السبحة في الشاشة الرئيسية؛ التطبيق لا يطلب
  `SYSTEM_ALERT_WINDOW` ولا يبدأ الخدمة من نفسه أبدًا (سياسة Play للصلاحية الخاصة).
- عدّاد دائري شبه شفاف + زر تصفير صغير؛ النقر يعدّ، **والضغط المطوّل 0.7ث على زر التصفير**
  يصفّر (لأن النقر المفرد Zufällig يفقد العدّ)، والضغط المطوّل **1.5ث على الدائرة** يقفل العدّ.
- السحب حر، وعند الإفلات قرب الحافة يلتصق بأقرب حافة بحركة انسيابية (ValueAnimator)،
  ويتلاشى إلى **35%** بعد 4 ثوانٍ سكون.
- اهتزاز `EFFECT_TICK` قصير ولطيف (وبدلًا منه oneShot 15ms على الأقدم).

### حماية الجيب والبطارية (ثلاث طبقات)
1. **مستشعر التقارب**: عند التغطية (جيب/وجه لأسفل) تُرفض كل اللمسات ويُوقف الاهتزاز فورًا.
2. **SCREEN_OFF**: إلغاء تسجيل المستشعر + حذف كل أعمال الـ Handler + إلغاء الاهتزاز →
   لا شيء يعمل ولا شيء يوقظ المعالج؛ و**USER_PRESENT** يعيد كل شيء.
3. **قفل يدوي** بالضغط المطوّل 1.5 ثانية.

### قرار معماري موثّق
خدمة **started عادية وليست Foreground**: على targetSdk 36 كانت Foreground ستفرض نوع خدمة
(`specialUse` = تبرير في Play Console) + إشعارًا دائمًا + صلاحية إشعارات على 13+.
الثمن المقبول: بعض واجهات OEM العدوانية (MIUI/EMUI) قد تقتلها — يُنصح المستخدم بتفعيل
"auto-start" إن أراد بقاءها دائمًا. لا wake lock ولا أي حلقة تشغيل: العمل المجدول الوحيد
هو مؤقّت تلاشي واحد مدته 4 ثوانٍ.

---

## 10) أذكار الصباح والمساء الآلية + التطبيق ثنائي اللغة (عربي/إنجليزي)

### الملفات الجديدة
| الملف | الدور |
|---|---|
| `res/raw/athkar.json` | قائمة الأذكار بكل الحقول: عربي بالتشكيل، ترجمة، نقل حرفي، مرجع باللغتين، وعدد التكرار — **أُعيدت كتابتها بالكامل في القسم 12** لتطابق إسلام بوك (31 صباح + 30 مساء) |
| `model/AthkarItem.java` + `model/AthkarFile.java` | نموذج Gson (المكتبة موجودة أصلًا بسبب TinyDB — بلا إضافة وزن) |
| `utils/PrayerWindow.java` | محرّك التوقيت: فلكي offline أو أوقات ثابتة، ويعيد `NONE / MORNING / EVENING` |
| `utils/AthkarRepository.java` | يقرأ JSON مرة واحدة لكل عملية ويصفّي حسب النافذة |
| `activity/AthkarActivity.java` + `adapter/AthkarAdapter.java` | القارئ بأسلوب BottomSheet + بطاقات بعدّاد تفاعلي |
| `utils/LocaleHelper.java` | مبدّل اللغة عبر `AppCompatDelegate.setApplicationLocales` |
| `res/values-ar/strings.xml` | الترجمة العربية الكاملة (101 نص، مطابقة تامة للإنجليزي) |
| `res/xml/locales_config.xml` | قائمة اللغات المدعومة (`en`, `ar`) للنظام |
| layouts/drawables | `activity_athkar`, `item_athkar`, `dialog_athkar_settings`, `bg_athkar_*`, `ic_language` |

### محرك التوقيت — بلا AlarmManager وبلا WorkManager إطلاقًا
القرار المعماري الأهم: **لا يوجد أي مجدول**. النافذة تُحسب *كسولًا* لحظة الحاجة إليها
(`onResume` في الرئيسية، ودورة إعادة البناء كل 60 ثانية في طبقة الخلفية). مؤقّت أو عامل
خلفية كان سيوقظ الجهاز ليحدّث شارة لا يراها أحد — استهلاك بطارية بلا فائدة.

- **الوضع الفلكي (الافتراضي)**: ميل الشمس + معادلة الزمن + الزاوية الساعية → الفجر، الشروق،
  العصر، المغرب، العشاء. المدينة هي نفسها المختارة لبوصلة القبلة (`qiblaCityIndex`)،
  وزاوية الفجر 15° أو 18° من الإعدادات.
- **النوافذ**: الصباح = الفجر → الشروق + ساعتين سماح؛ المساء = العصر → العشاء
  (والمغرب + ساعتين إذا تعذّر حساب العشاء).
- **الوضع الثابت**: أربعة أوقات يحددها المستخدم بـ `TimePickerDialog`
  (`morningStartMin/EndMin`, `eveningStartMin/EndMin`) لمن يفضّل جدوله الخاص.
- **خطوط العرض العالية**: إذا أرجعت المعادلات `NaN` (الشمس لا تبلغ الارتفاع المطلوب) يسقط
  المنطق تلقائيًا إلى الفترات الافتراضية بدل إخفاء الأذكار نهائيًا.
- **تحقّق رقمي فعلي** (13 سبتمبر، عمّان 31.9532/35.9106، +3): الفجر 05:12 · الشروق 06:20 ·
  العصر 16:02 · المغرب 18:43 · العشاء 19:51 — مطابقة للتقويم؛ ونافذة الصباح 05:12–08:20
  والمساء 16:02–19:51.

### الشارة
- تظهر في **الشاشة الرئيسية** (زر ذهبي أسفل الشاشة) و**على الخلفية الحية نفسها**
  (سطر ذهبي تحت التاريخ الهجري مباشرة، أو أعلى الشاشة إن كان الهجري مخفيًا).
- خارج النافذتين تختفي تمامًا، ويمكن إطفاؤها نهائيًا من
  *خيارات الخلفية → إظهار شارة الأذكار* (`showAthkarBadge`).
- `AthkarActivity` يعيد حساب النافذة عند الدخول ويُغلق نفسه إن انتهت — حماية من شارة قديمة.

### القارئ
- Activity شفافة بوزنَين (22% منطقة لمس للإغلاق + 78% اللوح) — نفس مظهر BottomSheet
  **بدون مكتبة material** (أُزيلت من المشروع عن قصد).
- كل بطاقة: النص العربي بالتشكيل (`textDirection="rtl"` + خط serif لتشكيل أدق)، النقل الحرفي،
  الترجمة، المرجع بلغة الواجهة، وعدّاد ذهبي يتناقص باللمس مع اهتزاز `EFFECT_TICK`،
  وعند الصفر يتلاشى الكرت إلى 45% وتظهر «تم ✓».
- **التقدّم في الجلسة فقط** (HashMap في الذاكرة، بلا حفظ): قرار مقصود — ورّد الأذكار عبادة
  لا «بيانات مستخدم»، والحفظ كان سيضيف سطح خصوصية وتخزينًا بلا قيمة.
- عدد التكرارات مُدار بالبيانات: إضافة ذكر بتكرار 100× لا تحتاج أي تعديل في الكود،
  فقط سطر في `athkar.json`.

### ثنائية اللغة و RTL
- **مبدّل اللغة**: أيقونة الكرة الأرضية في الشريط العلوي للرئيسية → اختيار English/العربية.
  الاعتماد على `AppCompatDelegate.setApplicationLocales` يعني أن النظام يتكفّل بإعادة إنشاء
  الواجهات ومرآة التخطيطات والحفظ.
- **الحفظ على API ≤ 32**: أُعلنت `AppLocalesMetadataHolderService` (معطّلة، بيانات وصفية فقط)
  في الـ Manifest وإلا ضاع اختيار اللغة بعد إغلاق العملية.
- **`android:localeConfig`** مضاف للتطبيق + `res/xml/locales_config.xml`.
- **تدقيق RTL شامل**: تحويل `marginLeft/Right → marginStart/End`،
  `paddingLeft/Right → paddingStart/End`، `gravity/layout_gravity left|right → start|end`،
  `alignParentLeft/Right`, `toLeftOf/toRightOf`, `drawableLeft/Right` في **15 تخطيطًا**،
  مع إزالة السمات المكررة الناتجة. لم يبقَ أي سمة اتجاهية مطلقة في المشروع.
- **محتوى الأذكار خارج `strings.xml`**: النص العربي والترجمة *محتوى* لا *واجهة*، لذلك يعيشان
  في `athkar.json` ولا يمسّهما مبدّل اللغة؛ المرجع وحده له نسختان (`reference_ar/en`).
- عناوين الواجهة بخط العرض `myfont` (Dela Gothic One): لا يملك حروفًا عربية، فيُكمل النظام
  العربية تلقائيًا بخط Naskh — مقروء وسليم، دون حزم خط عربي جديد (قرار: لا Amiri لتوفير الحجم).

### إصلاح جانبي مهم
أُضيف `TinyDB.getBoolean(String key, boolean fallback)` — كان `FrameRate` (القسم 8) يستدعي
نسخة بمعاملين غير موجودة، أي **خطأ تجميع كان كامناً**. الآن كل استدعاءات `TinyDB`
في المشروع مطابقة للتواقيع المعلنة (فحص آلي لكل الاستخدامات).

### الفحص
- مدقّق ثابت شامل (`.arena/verify.py`، خارج git): صحة كل XML، وجود كل مرجع
  `@string/@drawable/@id/@style/@font/@array/@raw`، كل `R.*` في Java، توازن الأقواس
  بمحلّل رموز حقيقي، وجود كل صنف في الـ Manifest، كل `findViewById`، وتطابق نصوص
  `values-ar` مع الإنجليزي (عددًا وأسماءً ووسائط `%1$d`). **النتيجة: لا أخطاء.**
  (java=60 · xml=81 · layouts=22 · drawables=212 · strings=101 · ids=112)
- لا إنترنت في وقت التشغيل إطلاقًا: كل شيء محلي (JSON داخل APK + حساب فلكي).

### اختبار يدوي مقترح بعد البناء
1. بدّل اللغة إلى العربية → تحقق من انعكاس التخطيطات كاملًا ومن بقاء النصوص الإنجليزية
   في مكانها الصحيح (النقل الحرفي).
2. افتح *أذكار → الإعدادات* وجرّب الوضع الثابت بأوقات حول اللحظة الحالية → يجب أن تظهر الشارة فورًا.
3. انقر عدّاد ذكر ×7 حتى الصفر → اهتزاز خفيف + تلاشٍ + «تم ✓».
4. أغلق التطبيق وافتحه بعد انتهاء النافذة → الشارة مخفية، وفتح القارئ يدويًا يُغلق نفسه.
5. فعّل/أطفئ *إظهار شارة الأذكار* من خيارات الخلفية → يختفي/يظهر السطر الذهبي على الخلفية الحية.

---

## 11) التعريب الكامل (والإنجليزية تبقى الافتراضية)

**القاعدة**: `values/` = الإنجليزية (الافتراضية)، `values-ar/` = العربية الكاملة. أي جهاز
لغة نظامه عربية يحصل على العربية **تلقائيًا**، ومبدّل اللغة داخل التطبيق يفرضها على الجميع.

### ما كان ما يزال بالإنجليزية وأصبح معرّبًا
1. **أسماء مدن بوصلة القبلة (73 مدينة)** كانت مكتوبة داخل `QiblaUtil.java`
   → انتقلت إلى `R.array.qibla_cities` (إنجليزي + عربي) مع دوال
   `cityNames() / cityName() / cityCount()` وحماية من فهرس خارج المدى.
   تم التحقق آليًا أن عدد الأسماء = عدد الإحداثيات (73 = 73) وأن الترتيب مطابق.
2. **أسماء الأشهر الهجرية** → `values-ar/islamic_content.xml`
   (محرم، صفر، ربيع الأول… ذو الحجة) بدل النقل اللاتيني.
3. **وجوه الساعات**: اسم اليوم والشهر وAM/PM كانت *مكتوبة يدويًا* بالإنجليزية
   (`"SUN"`, `"SEP"`, `"AM"`) وتُجلب عبر `SimpleDateFormat` **بلغة الجهاز لا لغة التطبيق**
   → أصبحت موارد: `clock_days`, `clock_days_short`, `clock_months`, `clock_am/clock_pm`
   (ص/م), `clock_day_date_format` (يعكس ترتيب اليوم والتاريخ حسب اللغة).
4. **`LocalizedTextPaint` (صنف جديد)**: الخطوط المدمجة (Montserrat/Rubik/Open Sans/
   Pathway Gothic/Dela Gothic) لا تملك أي حرف عربي، والكلمات العربية أعرض من `SUN`/`SEP`
   بينما التصاميم تضعها في خانات ثابتة حول القرص. الحل في **صنف واحد** بدل تعديل
   21 فرع تصميم: `setTypeface()` يستبدل الخط بوجه نظامي يشكّل العربية (مع الحفاظ على
   السماكة)، و`setTextSize()` يصغّر النص (0.78 للوجوه الرقمية، 0.85 للتناظرية) فيبقى
   داخل خانته. اللغة تُقرأ في كل استدعاء، فالتبديل يسري فورًا.
5. **السياقات غير الشاشات تتبع لغة التطبيق الآن**: `LocaleHelper.wrap()` طُبّقت على
   `LiveClockWallpaper` و`CustomWallpaper` و`WidgetClockRenderer` و`ClockWidgetProvider`
   (التاريخ الهجري + الذكر + صورة الساعة) ورسائل السبحة العائمة.
   بدونها كان Android 12 وأدنى سيجعل الخلفية والودجت يتحدثان الإنجليزية بينما التطبيق عربي
   (AppCompat هناك يبدّل لغة `AppCompatActivity` فقط).
6. **قارئ الأذكار**: النقل الحرفي يُخفى في الواجهة العربية (لا يفيد إلا من لا يقرأ العربية)
   وتبقى الترجمة الإنجليزية؛ وتسميات الأوقات تتبع لغة الواجهة.
7. **اتجاهات البوصلة** كانت `{"N","E","S","W"}` مكتوبة داخل `CompassView.java`
   → أصبحت `R.array.compass_cardinals` (شمال/شرق/جنوب/غرب بالعربية، وكلمات كاملة لأن
   «شرق» و«شمال» يشتركان في الحرف الأول). تُقرأ مرة وتُخزَّن، ولا يُعاد قراءتها إلا عند
   تغيّر اللغة — مهم لأن البوصلة تُعاد رسمها مع كل حدث مستشعر.

### إصلاحان بصريان اكتُشفا أثناء بناء معاينة التصميم
- **شاشة القبلة كانت شبه مخفية**: الخلفية `@color/themeDarkColor` (#dedede رمادي فاتح) بينما
  `CompassView` ترسم الحلقة والعلامات والحروف بأبيض شفاف والإبرة بالذهبي، والعناوين
  `@color/textColor` (أبيض) → تباين شبه معدوم. أُضيف `@color/qiblaBg` (#101820) وهو نفس
  أسود لوحة الأذكار الذي صُمّمت له هذه العناصر.
- **`TinyDB.getBoolean(key, default)`** (القسم 10) — كان `FrameRate` يستدعيها وهي غير موجودة.

### فرض التطابق آليًا
المدقّق الثابت يفحص الآن **كل ملفات `values/`** لا `strings.xml` وحدها: كل نص افتراضي يجب أن
يوجد في `values-ar` بنفس وسائط التنسيق (`%1$d`) وبشرطة apostrophe مُهرَّبة، وكل مصفوفة يجب أن
توجد بنفس عدد العناصر.
**النتيجة: 104 نصًا + 6 مصفوفات — لا أخطاء.**
(`qibla_cities` 73/73 · `clock_months` 12/12 · `clock_days` 7/7 · `clock_days_short` 7/7 ·
`hijri_months` 12/12 · `adhkar` مشتركة عن قصد)

### استثناءات مقصودة (بصراحة)
- **محتوى الإعلانات**: لغته من شبكة AdMob لا من التطبيق (والاختبارية تظهر بالإنجليزية).
- **أرقام الساعة**: لاتينية، لأن خطوط التصاميم لا تملك أرقامًا عربية-هندية (٠١٢٣) وتبديلها
  كان سيكسر شكل 21 تصميمًا. الأرقام في بقية التطبيق (الاتجاه، المسافة، الهجري) تتبع اللغة.
- **مصفوفة `adhkar`**: عربية في اللغتين — نص عبادة لا واجهة.
- **خط العناوين** `Dela Gothic One` بلا حروف عربية، فيُكمل النظام العربية بخط Naskh تلقائيًا.
  إن أردت لاحقًا خط عرض عربيًا حقيقيًا: ضع ملفًا واحدًا في `res/font-ar/myfont.ttf`
  وستلتقطه **كل** العناوين في الواجهة العربية دون سطر كود واحد.
- على Android 12 وأدنى: إن بدّلت اللغة والخلفية الحية تعمل، تبقى الخلفية على اللغة السابقة
  حتى إعادة تشغيل خدمة الخلفية (شاشات التطبيق والودجت يتحدثان فورًا). من 13+ فوري بالكامل.

### اختبار يدوي مقترح
1. بدّل إلى العربية من أيقونة الكرة الأرضية → تفقّد: الرئيسية، المحرر، الخلفيات، القبلة
   (أسماء المدن عربية)، السبحة، الأذكار.
2. اختر ساعة رقمية وأخرى ذكية وثالثة تناظرية → يجب أن يظهر اسم اليوم والشهر بالعربية
   داخل القرص (و«ص/م» بدل AM/PM) دون أن يخرج النص عن مكانه.
3. انظر إلى الخلفية الحية والودجت → التاريخ الهجري بالعربية (١٢ رمضان ١٤٤٧ هـ) والذكر عربي.
4. أرجع اللغة إلى English → كل ما سبق يعود إنجليزيًا فورًا.

---

## 12) مواءمة الأذكار مع إسلام بوك: المحتوى والعدد والتنسيق

طلبك: «جميل تعديل الأذكار مثل هذه والعدد والكم والتنسيق» — أي أن تكون الأذكار
مطابقة لقائمة **islambook.com** في النص والترتيب وعدد التكرار وطريقة العرض.

### ما نُقل حرفيًا من الموقع
| | الصباح | المساء |
|---|---|---|
| عدد الأذكار | **31** | **30** |
| المصدر | `islambook.com/azkar/1` | `islambook.com/azkar/2` |
| مجموع النقرات لإتمام القائمة | 374 | 274 |
| أذكار معها سطر «الفضل» | 15 | 15 |
| الترقيم في الملف | `id` من 1 إلى 31 | `id` من 101 إلى 130 |

**تكرارات الموقع كما هي** (لا تقريب ولا تعديل):

- الصباح: `1, 3, 3, 3, 1, 1, 3, 4, 1, 7, 3, 1, 1, 3, 3, 3, 1, 3, 1, 1, 3, 10, 3, 3, 3, 3, 1, 1, 100, 100, 100`
- المساء: `1, 1, 3, 3, 3, 1, 1, 3, 4, 1, 7, 3, 1, 1, 3, 3, 3, 1, 3, 1, 1, 3, 10, 3, 3, 3, 3, 100, 1, 100`

النص العربي منقول **بالتشكيل كما هو في الموقع** بما فيه:
- سطر `أَعُوذُ بِاللهِ مِنْ الشَّيْطَانِ الرَّجِيمِ` قبل آية الكرسي (وقبل آخِر البقرة في المساء)
  وسطر `بِسْمِ اللهِ الرَّحْمنِ الرَّحِيم` قبل الإخلاص والفلق والناس — كسطر مستقل ثم فراغ،
  تمامًا كتنسيق الموقع (9 أذكار متعددة الأسطر).
- المرجع بين قوسين مربّعين كما يظهره الموقع: `[آية الكرسى - البقرة 255]` · `[البقرة 285 - 286]`.
- فروق الصباح/المساء محفوظة: «أصبحنا/أمسينا»، «هذا اليوم/هذه الليلة»،
  «أصبحت أشهدك/أمسيت أشهدك»، «وإليك النشور/وإليك المصير»،
  و«خير هذا اليوم فتحه ونصره» مقابل «خير هذه الليلة فتحها ونصرها».
- آخِر البقرة (285-286) موجودة في **المساء فقط** كما في الموقع، و«علماً نافعاً ورزقاً طيباً
  وعملاً متقبلاً» و«أستغفر الله وأتوب إليه (100)» في **الصباح فقط** — ولهذا الصباح 31 والمساء 30.

### ما أُضيف في الكود لعرض التنسيق الجديد
| الملف | التغيير |
|---|---|
| `model/AthkarItem.java` | حقلان جديدان `virtue_ar` / `virtue_en` + `getVirtue(arabicUi)` (يُرجع نصًا فارغًا حين لا يوجد فضل، فيُخفى السطر) |
| `layout/item_athkar.xml` | سطر **رقم الذكر** في أعلى البطاقة، وسطر **الفضل** بلون ذهبي هادئ تحت الترجمة، وسطر المرجع صار أبيض شفافًا (الذهبي للفضل) |
| `layout/activity_athkar.xml` | شارة **عدد الأذكار** بجانب العنوان («31 ذكرًا» / «31 athkar») |
| `adapter/AthkarAdapter.java` | يعرض «الذكر X من Y»، والفضل (يُخفى إن كان فارغًا)، والتقدّم بصيغة **الباقي / الإجمالي** بدل «× N»، ويصغّر خط العدّاد إلى 13sp عندما يكون الرقم 100 (ثلاثة أرقام في دائرة 46dp) |
| `values{,-ar}/strings.xml` | `athkar_position` · `athkar_progress` · `athkar_count`، وحُذف `athkar_repeat_total` الذي لم يعد مستعملًا |

### لماذا سطر «الفضل» مهم
الموقع يطبع تحت الذكر نصًّا مثل «من قالها حين يصبح أجير من الجن حتى يمسى» — هذا ليس
مرجعًا ولا ترجمة، بل **فضل الذكر**، فأصبح حقلًا مستقلًا في النموذج لا نصًا ملصوقًا بآخر
حقل (وإلا لضاع عند التبديل بين اللغتين).

### توليد المحتوى وتحقّقه
المولّد **داخل المستودع** في `tools/athkar/` (لا في مجلد مؤقت) حتى يبقى المحتوى قابلاً
لإعادة البناء والتدقيق لاحقًا — `python3 tools/athkar/build_athkar.py` يعيد إنتاج نفس
الملف بايت ببايت:
- `tools/athkar/athkar_shared.py`: النصوص المشتركة بين الصباح والمساء (24 ذكرًا) مكتوبة
  مرة واحدة، فلا يمكن أن يختلف نص أو ترجمة أو نقل حرفي بين القائمتين.
- `tools/athkar/athkar_lists.py`: ترتيب القائمتين + الأذكار الخاصة بكل وقت (منقولة حرفيًا).
- `tools/athkar/README.md`: البنية والفحوص وكيفية إضافة ذكر.
- `tools/athkar/build_athkar.py`: يبني `res/raw/athkar.json` ويرفض البناء إن فشل أي فحص:
  كل الحقول موجودة، `repeat ≥ 1`، النص العربي يحوي حروفًا عربية فعلًا، الترجمة والنقل
  الحرفي خاليان من الحروف العربية، الفضل والمرجع موجودان في اللغتين معًا أو غائبان معًا،
  31 صباحًا و30 مساءً بالضبط، ولا `id` مكرر، ولا حروف `<` أو `>` أو `&` أو `"` في أي نص
  (المعاينة تبني البطاقات بـ `innerHTML`).
  **النتيجة: 61 ذكرًا، 66 كيلوبايت — كل الفحوص ناجحة.**

### المعاينة التفاعلية
- `assets/data.js` أُعيد بناؤه (61 ذكرًا، 86 ك.ب) و`renderAthkar()` يعرض الآن: رقم الذكر،
  النص متعدد الأسطر (`white-space: pre-wrap`)، سطر الفضل، المرجع، و«الباقي / الإجمالي».
- **إصلاح سلوكي**: النقر على العدّاد كان يعيد رسم القائمة كاملة فيقفز القارئ إلى أعلى
  الشاشة — صار الآن يعدّل البطاقة المنقورة وحدها في مكانها، وهو ضروري مع 31 بطاقة
  (بعضها 700 حرف) وإلا صار إتمام القائمة مزعجًا.

### ملاحظات أمانة
- النصوص منقولة كما هي في الموقع **بأخطائه المطبعية أيضًا** (مثل «عَلَى كُلِّ شَيْءِ قَدِيرِ»
  بالجر في الذكر 29 صباحًا و28 مساءً) — طلبك كان المطابقة، والتعديل الصامت على نص
  منقول أسوأ من إبقائه كما في المصدر.
- المراجع الحديثية (`[البخاري]`، `[مسلم]`، `[أبو داود]` …) ليست معروضة في الموقع؛ أُضيفت
  لأن سطر المرجع موجود أصلًا في التصميم، وكل مصدر مذكور هو المصدر المعروف لذلك الذكر.
- أذكار الخلفية الحية الدوّارة (`R.array.adhkar`) تُركت كما هي عن قصد: سبعة أذكار قصيرة
  تُقرأ بلمحة من الشاشة الرئيسية، وكلها ضمن قوائم إسلام بوك أصلًا.

### اختبار يدوي مقترح
1. افتح قارئ الأذكار في نافذة الصباح → الشارة تُظهر «31 ذكرًا»، وأول بطاقة هي آية الكرسي
   بسطر الاستعاذة قبلها، وتحتها «من قالها حين يصبح أجير من الجن…»، والعدّاد «1 / 1».
2. اقلب إلى المساء → «30 ذكرًا»، والبطاقة الثانية هي آخِر البقرة (غير موجودة في الصباح).
3. انقر بطاقة الإخلاص ثلاث مرات → «0 / 3» ثم تتلاشى وتظهر «تم ✓».
4. انقر «لا إله إلا الله وحده…» (100) → الرقم يظهر بثلاثة أرقام داخل الدائرة دون أن يخرج عنها.
5. بدّل اللغة إلى English → الفضل والمرجع يظهران بالإنجليزية ويُخفى النقل الحرفي في العربية.

---

## 13) قارئ الأذكار: صفحة ورقية كلاسيكية، والنقر على الذكر نفسه

ملاحظتك: «خلفية الأذكار باللون الأخضر ليست جميلة، لو بدون خلفية المسجد ولا ألوان يكون
كلاسيك. والنقر لا يجب أن يكون على الدائرة، العدد على الذكر».

### تشخيص أولًا (بأمانة)
لم يكن في القارئ أي لون أخضر في الكود: اللوحة كانت `#F2101820` (شبه أسود) والبطاقات بيضاء
شفافة والعدّاد ذهبي و«تم ✓» أخضر مزرقّ `#80CBC4`. **الأخضر الذي رأيته هو صورة المسجد** —
الخلفية المختارة كانت تظهر حول اللوحة ومن خلفها (بعض الخلفيات قبب ومروج خضراء). لذلك
الحل ليس تبديل لون بل إلغاء الفكرة كلها: لا صورة خلف الأذكار.

### التصميم الجديد
| | قبل | بعد |
|---|---|---|
| الشكل | لوحة عائمة فوق الخلفية الحية (78% من الشاشة) | **صفحة كاملة معتمة** بلا خلفية حية |
| الخلفية | صورة مسجد + لوح شبه أسود | **ورق** `#FBF8F2` سادة |
| الألوان | ذهبي (العدّاد) + أخضر مزرقّ (تم) + بنفسجي (المرجع) | **حبر ورمادي فقط** — لا لون واحد |
| النقر | على الدائرة الذهبية (46dp) | **على الذكر كله** (البطاقة كاملة هدف اللمس) |
| العدد | داخل الدائرة | **بجانب نص الذكر** + «الباقي / الإجمالي» في سطر الرأس |
| الإغلاق | بالنقر فوق اللوحة | زر ✕ صريح في الشريط العلوي |

اللوحة اللونية الجديدة في `colors.xml` (كلها محايدة):
`athkarPaper #FBF8F2` · `athkarInk #1B1B1B` · `athkarBody #3D3A35` ·
`athkarMuted #6E6A63` · `athkarSoft #8C867C` · `athkarHairline #E3DDD1`.

### ترتيب البطاقة (كما طلبت: العدد في المكانين)
1. سطر رأس صغير رمادي: «الذكر 5 من 31» … «3 / 3» … («تم ✓» عند الإكمال).
2. **رقم التكرار في عمود بجانب نص الذكر** (كما يطبعه إسلام بوك في عموده) — يتناقص مع كل نقرة.
3. النص العربي 20sp بحبر أسود وتبديل أسطر 8dp (أوضح على الورق منه على الأسود).
4. النقل الحرفي (يُخفى في الواجهة العربية) ثم الترجمة ثم **سطر الفضل** ثم المرجع.
5. عند الإكمال يبهت السطر كله إلى 40% — لا لون ولا شارة.
6. بين كل ذكرين **خط فاصل شعري** `1dp`، والنقر يعطي تموّجًا رماديًا (`selectableItemBackground`).

### السمة والحواف
- `AppTheme.AthkarSheet` (شفافة + تعتيم + حركة حوار) → **`AppTheme.AthkarReader`**: صفحة
  معتمة، `windowBackground = @color/athkarPaper`، وبلا حركة حوار.
- `AthkarActivity` تستدعي `UiCompat.applyEdgeToEdge()` ثم تعيد طلاء خلفية النافذة بالورقي:
  الدالة تصبغها أبيض لشاشات القوائم، فبدون هذه السطرة يظهر شريط بلون مختلف فوق الشريط العلوي.
  وهي نفسها تجعل أيقونات شريط الحالة **داكنة** — ضروري على الورق الفاتح.

### خطأ حقيقي اكتُشف أثناء التعديل وأُصلح
**حوار «إعدادات توقيت الأذكار» كان نصّه أبيض على أبيض**: `AlertDialog` من سمة
`Theme.AppCompat.Light` خلفيته بيضاء، بينما الحوار يستخدم `@color/textColor` (أبيض) لتسميات
الاختيار وللساعات الأربع، و`bg_athkar_card` (أبيض شفاف 8%) للبلاطات — أي أن كل محتوى الحوار
كان غير مقروء عدا العنوان الأزرق. صار الحوار ورقيًا هو الآخر: حبر على أبيض، تسميات رمادية،
وبلاطات الوقت بإطار شعري (`bg_athkar_chip.xml` جديد).

### تنظيف
حُذفت ثلاثة موارد ماتت بالتصميم الجديد: `bg_athkar_sheet.xml` · `bg_athkar_counter.xml`
(الدائرة الذهبية) · `bg_athkar_card.xml` (الأبيض الشفاف). لم يبقَ أي مرجع لها — المدقّق
يتحقّق من كل `R.drawable`.

### المعاينة التفاعلية
شاشة «قارئ الأذكار» في المعاينة صارت مطابقة: صفحة ورقية، شريط علوي (✕ · العنوان · «31 ذكرًا» ·
⚙)، أسطر بفواصل شعرية، الرقم بجانب الذكر، والنقر على أي مكان في السطر. وشاشة «إعدادات
توقيت الأذكار» صارت حوارًا فاتحًا فوق الصفحة الورقية بدل الحوار الداكن الذي كان يخفي أن
النص الحقيقي أبيض على أبيض. (أيقونات الشريط تُصبغ بـ `currentColor` كما يفعل `app:tint`.)

### ما بقي ذهبيًا عن قصد
**شارة «أذكار الصباح/المساء»** على الخلفية الحية والشاشة الرئيسية ما زالت ذهبية
(`bg_athkar_badge`): هي فوق صورة، والذهبي لغة التطبيق المميزة (الإبرة، السبحة، الساعات
المقفلة). قل «اجعلها ورقية» وأقلبها إلى حبة بيضاء بحبر أسود في سطر واحد.

### اختبار يدوي مقترح
1. افتح القارئ → صفحة ورقية بلا أي صورة أو لون، وأيقونات شريط الحالة داكنة ظاهرة.
2. انقر **في أي مكان** من ذكر «قُلْ هُوَ ٱللَّهُ أَحَدٌ» → الرقم بجانب النص 3 ← 2 ← 1 ← 0
   و«3 / 3» في الأعلى تصير «0 / 3»، ويبهت السطر وتظهر «تم ✓».
3. اذكر بتكرار 100 → الرقم ثلاثي الخانات في عموده دون أن يزحزح النص.
4. افتح ⚙ → الحوار مقروء: التسميات والساعات الأربع بحبر أسود على أبيض (كانت بيضاء على بيضاء).
5. أغلق بـ ✕ → تعود للخلفية، والشارة الذهبية ما زالت هناك.

---

## 14) أدوات التحقق صارت داخل المستودع (لا في مجلد مؤقت)

المعاينة التفاعلية والمدقّق الثابت كانا في مجلد عمل مؤقت لا يُحفَظ مع المستودع، فضاعا عند
إعادة تشغيل البيئة وبقي التطبيق وحده. نُقلا إلى `tools/` وأُعيد بناؤهما، فصارا جزءًا من
المشروع يُشغَّل في أي وقت:

| الأداة | الأمر | الدور |
|---|---|---|
| `tools/athkar/build_athkar.py` | `python3 tools/athkar/build_athkar.py` | يبني `res/raw/athkar.json` (61 ذكرًا) ويرفض البناء إن فشل أي فحص محتوى |
| `tools/verify_resources.py` | `python3 tools/verify_resources.py` | مدقّق الموارد الثابت — بديل عن البناء لأنه **لا JDK في بيئة العمل** |
| `tools/preview/build_assets.py` | `python3 tools/preview/build_assets.py` | يولّد أصول المعاينة من `res/` (نصوص، مصفوفات، أذكار، ألوان، أيقونات، خلفيات، تصاميم) |
| `tools/preview/server.js` | `node tools/preview/server.js 8080` | يخدم المعاينة على `0.0.0.0:8080` |
| `tools/preview/smoke.js` | `node tools/preview/smoke.js` | يشغّل `app.js` على DOM مصغّر داخل Node ويفحص 22 أمرًا |

### ماذا يفحص `verify_resources.py`
1. كل ملفات `res/**/*.xml` تُحلَّل دون خطأ (85 ملفًا).
2. `values-ar` تحمل **كل** نص افتراضي (106/106) بنفس وسائط التنسيق (`%1$d`) — وأي نص عربي
   بلا أصل إنجليزي يُعدّ خطأ.
3. الشرطة المائلة قبل الـ apostrophe كما يطلب `aapt2` (إلا داخل نص محاط بعلامتي اقتباس).
4. كل مصفوفة موجودة في اللغتين تملك **نفس عدد العناصر** (و`adhkar` عربية عن قصد في اللغتين).
5. كل `R.string/R.id/R.array/R.drawable/R.color/R.raw/...` مستعمل في جافا (61 ملفًا) مُعلن فعلًا.
6. كل `@type/name` داخل التخطيطات والرسومات يُحلّ إلى مورد موجود.
7. كل `findViewById(R.id.x)` له `@+id/x` في تخطيط ما.
8. توازن أقواس جافا — بماسح أحرف لا بتعبير نمطي، لأن رابطًا داخل نص (`"https://…"`)
   يبدو كتعليق سطر، وشرطة داخل تعليق تبدو كثابت حرفي (كلاهما كسر العدّ أول مرة).

**النتيجة على الشجرة الحالية: لا أخطاء.** وللتأكد أن المدقّق ليس «يوافق دائمًا»، كُسرت
الشجرة عمدًا ثلاث مرات فالتقطها: قوس ناقص في `QiblaUtil`، و`R.string` غير موجود،
وحذف نص عربي + كسر وسيط تنسيقه. ثم أُعيدت الملفات من git.

### المعاينة بعد إعادة البناء
12 شاشة كما كانت، لكنها الآن تُظهر **صور التصاميم الحقيقية** (`clock_bg_*` و`digital_thumb_*`
و`smart_thumb_*`) بدل الرسوم التقريبية، وتحسب اتجاه القبلة ومسافتها من إحداثيات
`QiblaUtil` نفسها (عمّان 161° و1234 كم)، وتحمّل خطوط العرض نفسها التي يحزمها التطبيق.
أصولها (`tools/preview/assets/`، 3.5 م.ب) متجاهَلة في git وتُعاد بأمر واحد.

---

## 15. حالة المستودع والتسليم (16 سبتمبر 2026)

### الفرع و`main`
كل العمل موثّق على الفرع `arena/01a0970d-newnew`. أُعيد نقل (rebase) 19 commit فوق
`main` مباشرة، فصار `main` أصلًا للفرع والدمج نظيفًا بلا تعارض — طلب الدمج
[PR #1](https://github.com/mo1980code-max/newnew/pull/1) حالته `MERGEABLE / CLEAN`.
قبل النقل كانت الشجرتان غير مرتبطتين تاريخيًا (`main` commit جذري واحد)، والفرق الوحيد
في المحتوى كان `settings.gradle`.

### من يملك إعلان المستودعات
`settings.gradle` (الآتي من `main`) يعلن `pluginManagement` و`dependencyResolutionManagement`
بنمط `RepositoriesMode.PREFER_SETTINGS`. هذا النمط **يتجاهل** أي مستودع مُعلن داخل
`build.gradle`، لذلك حُذف كتلة `allprojects { repositories { ... } }` من `build.gradle`
الجذر وصارت الإشارة إلى `settings.gradle` وحده (فيه `google()` و`mavenCentral()` و`jitpack.io`
اللازمة لـ `com.github.QuadFlask:colorpicker:0.0.14`). كتلة `buildscript` لم تُمسّ:
مسار classpath للإضافات يُحلّ مستقلًا عن `dependencyResolutionManagement`.

### حزمة التنزيل ولماذا هي داخل المستودع
`Allah-Clock-Live-Wallpaper-android-studio.zip` (9.2 م.ب / 358 ملفًا) محفوظ **داخل المستودع**
على جذر الفرع، لأن بيئة البناء لا تصل إلى `uploads.github.com` (فشل TLS فوري) فمرفقات
Releases غير ممكنة. يُعاد بناؤه بالأمر:

```bash
python3 tools/build_package.py
```

وهو يستثني `.git` وكل `build/` و`.gradle/` و`local.properties` وأصول المعاينة المولَّدة
`tools/preview/assets/` وأي `*.zip` (حتى لا تبتلع الحزمة نسخةً منها تُقدَّم عبر خادم
المعاينة)، ويحفظ صلاحية `gradlew` = 0755 داخل الأرشيف.

> بعد أي تعديل على المصدر: `python3 tools/build_package.py` ثم الالتزام، حتى تبقى الحزمة
> مطابقة للشجرة. النسخة المقدَّمة عبر خادم المعاينة تُنسخ يدويًا:
> `cp Allah-Clock-*.zip tools/preview/` (وهي متجاهَلة في git).

### فحص التسليم الأخير
| الفحص | النتيجة |
|---|---|
| `tools/verify_resources.py` | NO ERRORS |
| بصمة blob للحزمة: محليًا مقابل GitHub API | `ced60e7a…c52abc` متطابقة (9,646,452 بايت) |
| سلامة الأرشيف وعدد مدخلاته | OK · 358 |
| المحتوى | 61 صنف جافا · 85 ملف res XML · 21 خلفية · 16 خطًا |
| خادم المعاينة | `/` و`/assets/data.js` والحزمة كلها 200 |

---

## 16) قارئ القرآن الكريم — فهرس، بحث، استئناف وعلامات محفوظة (16 سبتمبر 2026)

أضيفت بطاقة **«القرآن الكريم»** واضحة في الواجهة الرئيسية، وتفتح قارئًا محليًا هادئًا بلا
إعلانات وبلا اتصال إنترنت. لا توجد تلاوات صوتية في هذه المرحلة؛ هذه ميزة **مصحف قراءة**.

### ما يراه المستخدم

1. **فهرس السور**: جميع السور الـ114، مع الاسم العربي والاسم اللاتيني/المعنى وعدد الآيات
   ومكية/مدنية.
2. **متابعة القراءة**: تحفظ الشاشة أول آية مرئية عند التمرير أو الخروج، وتظهر بطاقة
   «متابعة القراءة» في الفهرس لتعيد القارئ مباشرة إلى السورة والآية نفسهما.
3. **بحث محلي**: يبحث في اسم السورة وفي نص جميع الآيات دون إنترنت؛ يتجاهل التشكيل والفروق
   الشائعة في الألف والياء، ويدعم مرجعًا مباشرًا مثل `2:255`.
4. **العلامات المحفوظة**: أيقونة العلامة بجانب كل آية تحفظها، والنقر المطوّل على بطاقة الآية
   يفعل الشيء نفسه. شاشة «العلامات» تستعيدها بعد إغلاق التطبيق، ترتب الأحدث أولًا، وتسمح
   بالانتقال إليها أو حذفها. تحفظ محليًا في `SharedPreferences` فقط، ولا تُرسل إلى أي خادم
   أو مزود إعلانات. تبقى حتى يحذفها المستخدم أو يمسح بيانات التطبيق/يلغيه.

### الملفات والمصدر

| المسار | المهمة |
|---|---|
| `res/raw/quran_uthmani.txt` | النص العثماني الكامل، 6,236 آية؛ مورد للقراءة فقط ولا يغيّر التطبيق نصه |
| `res/raw/quran_surahs.tsv` | بيانات فهرس السور الـ114 |
| `res/raw/quran_uthmani_license.txt` | إشعار ترخيص النص الكامل |
| `activity/QuranIndexActivity.java` | الفهرس، الاستئناف، نافذة المصدر |
| `activity/QuranReaderActivity.java` | القراءة وحفظ موضع القراءة |
| `activity/QuranSearchActivity.java` | البحث المحلي في النص والفهرس |
| `activity/QuranBookmarksActivity.java` | استرجاع العلامات وحذفها |
| `utils/QuranRepository.java` | تحميل المورد والتحقق من 114/6,236 والبحث |
| `utils/QuranStore.java` | تخزين الموضع والعلامات محليًا |

النص هو **Tanzil Quran Text (Uthmani, Version 1.1)**، Copyright © 2007–2024 Tanzil Project،
بترخيص Creative Commons Attribution 3.0. نُسخت النسخة حرفيًا من المصدر المراجع ولم يُعدّل
نص الآيات. تظهر نسبة المصدر في الواجهة، ويوجد الإشعار الكامل في المورد، وتفاصيل المصدر
والبصمة في [`docs/QURAN_TEXT_ATTRIBUTION.md`](docs/QURAN_TEXT_ATTRIBUTION.md).

### سلامة النص قبل النشر

امتد `tools/verify_resources.py` الآن بفحص ثامن خاص بالمصحف: يتأكد من 114 سورة و6,236 آية
وترتيب كل الآيات وعددها، ومن إشعار Tanzil، ومن SHA-256 للنص:

```text
f64fe7657dbe2e185e9995e14f7a67ee6cf1a30773f39184883d7a763d70fb19
```

لا تستبدل النص أو تعدله يدويًا. عند اعتماد تحديث موثق من Tanzil: راجعه مستقلًا أولًا ثم حدّث
النص والبصمة في المدقق وملف النسبة معًا.

---

## 17) وضع المصحف بالصفحات وإتاحة حجم النص (16 سبتمبر 2026)

اكتمل القارئ بوضع **«المصحف»** القابل للقراءة صفحةً بصفحة، مع الحفاظ على قارئ السور والبحث
والعلامات القائمة. لا يدّعي هذا الوضع أنه صورة أو PDF للمصحف المرفوع غير المتاح في شجرة
المشروع؛ بل يعيد تدفّق النص العثماني الموثّق ليلائم الشاشة مع الالتزام بحدود صفحات المصحف
المدني التقليدية.

### ما أضيف

1. **604 صفحة معيارية و30 جزءًا**: الموردان `res/raw/quran_pages.tsv` و`res/raw/quran_juz.tsv`
   يسجلان بداية كل صفحة وجزء بمرجع السورة والآية، مستخرجين من `QuranData.Page` و`QuranData.Juz`
   في نسخة Tanzil المراجعة نفسها. يستبعدان عمدًا فهرس JavaScript الفارغ والحارس `[115, 1]`
   بعد آخر حد.
2. **تنقل عملي**: بطاقة «قراءة المصحف بالصفحات» في الفهرس، وأزرار السابق/التالي، ولمس رقم
   الصفحة للانتقال المباشر إلى أي صفحة من 1 إلى 604 أو لمس شارة الجزء لاختيار أحد الأجزاء
   الثلاثين. زر المصحف في قارئ السور يفتح الصفحة التي تقع فيها أول آية ظاهرة حاليًا.
3. **استئناف وعلامات مرتبطة بالصفحة**: يبقى التخزين المرجعي الدقيق `سورة:آية` كما هو، ثم
   يحوّله `QuranRepository` إلى رقم صفحة ثابت عند عرض «متابعة القراءة» وعند استعادة العلامات؛
   لذلك لا تتضرر العلامات عند انتقال الآية بين وضع السورة ووضع المصحف.
4. **حجم نص قابل للحفظ**: زر حجم النص في وضعي القراءة يتيح اختيار 18–34sp. يحفظ الاختيار
   محليًا ويطبقه على الوضعين دون تغيير النص العثماني نفسه.
5. **سلامة وحدود صريحة**: يُظهر الوضع نص الآيات المتدفق وفق حجم المستخدم، وليس محاكاةً
   لتخطيط الحروف في طبعة مادية. عند توفر ملف المصحف الذي طلبه المستخدم فعلًا، يلزم فحص
   حقوقه ودمجه كأصل منفصل قبل وصف التطبيق بأنه يستخدمه «كما هو».

### ملفات التغيير

| المسار | المهمة |
|---|---|
| `res/raw/quran_pages.tsv` | 604 حدود صفحات مدنية مع نسبة Tanzil وبصمة موثقة |
| `res/raw/quran_juz.tsv` | 30 بداية جزء للتنقل المباشر مع نسبة Tanzil وبصمة موثقة |
| `model/QuranPage.java` و`model/QuranJuz.java` | نطاق آيات الصفحة وبدايتها ونهايتها، وبداية الجزء وصفحته |
| `activity/QuranMushafActivity.java` | شاشة القراءة بالصفحات والتنقل والاستئناف |
| `activity/QuranReaderActivity.java` | انتقال من قارئ السورة إلى الصفحة المطابقة وحجم النص |
| `utils/QuranRepository.java` | تحميل/تحقق حدود الصفحات وربط كل آية بصفحتها |
| `utils/QuranStore.java` | حفظ حجم نص القارئ محليًا |
| `adapter/QuranAyahAdapter.java` | تطبيق حجم النص المختار دون استبدال النص |

امتد المدقق إلى فحص أن موردي الصفحات والأجزاء يحتويان بالضبط على 604 و30 بداية مرتبة، وأن
كلًا منهما يغطي 6,236 آية مرة واحدة. بصمتاهما هما:

```text
946e458e8866da0621c172579e825c352d86b892f530b282ff2636276d37088a
9c9b80824ddc8bfa16da5bbb59433441f233611843f0ee4d1c44fc05b3794f18
```

---

## 18) جلسة التثبيت: إصلاحات الإعلانات + تنظيف + README (17 سبتمبر 2026)

طلب الجلسة: إصلاح/تحسين + إعلانات/AdMob + توثيق/تنظيف + «إضافة القرآن».
الفحص أظهر أن القرآن **موجود ومكتمل أصلًا** (الأقسام 16–17: قارئ سور ومصحف
بالصفحات وبحث وعلامات واستئناف، نص Tanzil العثماني ببصمات موثقة، ومدخل من
الشاشة الرئيسية) والمدقق يعطي `NO ERRORS` — فتركّز العمل على إصلاحات حقيقية
وُجدت بالمراجعة اليدوية، لا على إعادة ما هو موجود.

### إصلاح 1: علم الإعلان المملوء الشاشة كان قد يعلق للأبد (AdMob)

`AdManager.FULL_SCREEN_ACTIVE` يمنع تكدّس إعلانات فتح التطبيق فوق interstitial
أو rewarded. لكن في مسارين كان العلم يُرفع ثم لا يُخفَض أبدًا إن رمى `show()`
استثناءً (يحدث مع WebView تالف أو Activity في طور التدمير):

- `showInterstitial`: الـ `catch` كان يستدعي `onFinished` دون تصفير العلم →
  إعلانات App Open تتوقف لبقية حياة العملية. **أُضيف التصفير في الـ `catch`.**
- `showRewarded`: لم يكن هناك `try/catch` حول `rewardAd.show()` أصلًا →
  العلم عالق **و** الـ callback لا يُستدعى أبدًا (المستخدم عالق بلا فتح وبلا
  رسالة). **لُفّ الاستدعاء بـ `try/catch` يصفّر العلم ويستدعي `settle`.**

### إصلاح 2: بطاقة الـ Native كانت تُمرَّر للمحوّل الداخلي

`NativeAdListAdapter` كان يمرّر حتى حامل بطاقة الإعلان (`NativeAdViewHolder`)
إلى دوال المحوّل المغلّف (`onViewAttachedToWindow` / `onViewDetachedFromWindow` /
`onViewRecycled` / `onFailedToRecycleView`) — وأي محوّل داخلي يحوّل النوع
(cast) في هذه الدوال كان سيسقط بـ `ClassCastException`. الآن تُمرَّر عناصر
المحتوى فقط، وبطاقة الإعلان تُعالَج محليًا.

### إصلاح 3: انحراف توثيق أرقام الإعلانات

ترويسة `AdConfig` كانت تقول «four ad-unit ids … Replace all five values» من زمن
أربع وحدات؛ الآن خمس وحدات (Banner / Interstitial / Rewarded / App Open /
Native) + رقم التطبيق = **ست قيم** يجب استبدالها قبل النشر. صُحّح النص.

### تنظيف: `printStackTrace` → `Log.w`

آخر 4 استخدامات لـ `printStackTrace()` في المشروع (داخل `drawFrame()` في
`CustomWallpaper` و`LiveClockWallpaper` — تُستدعى مع كل إطار!) صارت
`Log.w(TAG, …)` برسالة سياقية، مع إضافة `import android.util.Log` الناقص.
لا `printStackTrace` ولا `TODO/FIXME` متبقٍ في `app/src/main/java`.

### توثيق: `README.md` جديد

جذر المستودع لم يكن فيه README إطلاقًا. أُضيف ملف عربي-أول مختصر: ما التطبيق،
جدول متطلبات البناء، أمر المدقق، قائمة ما قبل النشر، الميزات، وخريطة البنية —
مع روابط للأقسام التفصيلية في `UPGRADE_NOTES.md` و`docs/`.

### القرآن في هذه الجلسة: تحقق لا إضافة

- المدقق يؤكد: 114 سورة، 6236 آية، 604 صفحة مدنية، 30 جزءًا، وبصمات Tanzil
  الثلاث مطابقة (`NO ERRORS`).
- رُوجعت شاشات القراءة يدويًا: الاستئناف، العلامات، حجم النص (18–34sp مع ضمّن
  في `QuranStore`)، `contentDescription` لكل الأزرار، وغياب الإعلانات عن كل
  شاشات القرآن — كلها سليمة، فلم يُمسّ نص القرآن أو عرضه.

### الفحص

```bash
python3 tools/verify_resources.py   # NO ERRORS
grep -rn "printStackTrace\|TODO\|FIXME" app/src/main/java/  # لا نتائج
```
